#include <bits/stdc++.h>

using namespace std;

int a[1005];

vector<int> adj[1005];

int main(){
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int n;
    cin >> n;
    for (int i = 1; i <= n; i++){
        cin >> a[i];
    }

    
    
}